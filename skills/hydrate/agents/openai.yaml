interface:
  display_name: "Hydrate"
  short_description: "Persistent hydration reminders for agent work"
  default_prompt: "Use $hydrate to keep hydration reminders active during work."
policy:
  allow_implicit_invocation: true
